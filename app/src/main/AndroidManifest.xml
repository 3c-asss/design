<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.design">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- 蓝牙模组 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" /> <!-- 仅在支持BLE（即蓝牙4.0）的设备上运行 -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" /> <!-- 如果Android 6.0 蓝牙搜索不到设备，需要补充下面两个权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 互联网 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 查看网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ValidationActivity"></activity>
        <activity android:name=".xinxi" />
        <activity android:name=".news" />
        <activity android:name=".activity_update" />
        <activity android:name=".activity_report" />
        <activity android:name=".activity_query" />
        <activity android:name=".activity_delete" />
        <activity android:name=".activity_insert" />
        <activity android:name=".chengyuanActivity" />
        <activity android:name=".splash_scteen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myselfActivity" />
        <activity android:name=".nav_MainActivity2" />
        <activity android:name=".lunboActivity2" />
        <activity android:name=".registation" />
        <activity android:name=".researchActivity2" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>